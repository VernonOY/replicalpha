# Reproduction Report

## Paper

- **Source**: Study of the Momentum Effect and the Reversal Effect on the Chinese Stock Market
- **Factors claimed**:

- **reversal_effect** (逆转效应) — formula: `-pct_change(close, 120)`

## Factor Code

Generated via **dsl** (✅ qtype clean).

```python
"""Factor: reversal_effect — generated by replicalpha (dsl).

compute() returns a dict[ticker, value] for the given ``as_of`` date.
"""
from __future__ import annotations

from datetime import date, timedelta

import pandas as pd


FACTOR_NAME = "reversal_effect"
LOOKBACK = 120


def compute(adapter, as_of: date, universe: str = "") -> dict[str, float]:
    """Cross-sectional factor values at ``as_of``."""
    start = as_of - timedelta(days=LOOKBACK * 3)
    end = as_of

    def _series(field: str) -> pd.DataFrame:
        raw = adapter.get_price(field, start, end, universe)
        return pd.DataFrame({t: pd.Series(v) for t, v in raw.items()})

    def _pct_change(s: pd.DataFrame, n: int) -> pd.DataFrame:
        return s.pct_change(int(n))

    def _rolling_mean(s: pd.DataFrame, n: int) -> pd.DataFrame:
        return s.rolling(int(n)).mean()

    def _rolling_std(s: pd.DataFrame, n: int) -> pd.DataFrame:
        return s.rolling(int(n)).std()

    def _rolling_sum(s: pd.DataFrame, n: int) -> pd.DataFrame:
        return s.rolling(int(n)).sum()

    def _corr(a: pd.DataFrame, b: pd.DataFrame) -> pd.DataFrame:
        return a.rolling(LOOKBACK).corr(b)

    def _rank(s: pd.DataFrame) -> pd.DataFrame:
        return s.rank(axis=1, pct=True)

    def _zscore(s: pd.DataFrame) -> pd.DataFrame:
        return s.sub(s.mean(axis=1), axis=0).div(s.std(axis=1).replace(0, 1), axis=0)

    df = -(_pct_change(_series("close"), 120))
    last = df.dropna(how="all").iloc[-1] if not df.empty else pd.Series(dtype=float)
    return {t: float(v) for t, v in last.items() if pd.notna(v)}
```

## Backtest

- Period: `2022-01-04` → `2024-12-31`
- IC mean: **-0.0218**  (std 0.3285)
- Cumulative return: **+4.46%**
- Max drawdown: **20.17%**
- Annualized Sharpe: **0.19**

## Red Team

### 🟡 `sample_concentration` — warning

year 2023 accounts for 59% of cumulative IC — performance is highly time-specific

*Suggestion*: check robustness across market regimes; consider walk-forward or regime-conditional evaluation

## Reproducibility

- Paper's claimed IC: **0.0131**
- Reproduced IC: **-0.0218**
- Sign match: 0
- Magnitude score: 0.00
- **Final score**: 0.00

> sign mismatch: paper claimed IC = 0.0131, reproduced = -0.0218. this is a red flag — strategy may not work as claimed

---

*Run `2960e76f`; stages complete: extract, codegen, backtest, validate, score, report. Generated by replicalpha v0.1.*
